### GENERATED BY PUPPET ###
<%
require 'yaml'
result = {'filebeat' => {'prospectors' => [] }}
@log_files_to_follow.each do |f|
  f.delete('input_type') if f.has_key?('input_type')
  out = {'input_type' => 'log'}.merge(f)
  result['filebeat']['prospectors'] << out
end
%>

<%= result.to_yaml %>
